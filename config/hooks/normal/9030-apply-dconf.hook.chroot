#!/bin/bash
set -e

echo "[HoodOS] Applying GNOME defaults..."

export HOME=/etc/skel
dbus-launch dconf load /org/gnome/ < /etc/skel/.config/dconf/user.d/hoodos-gnome.conf || true
glib-compile-schemas /usr/share/glib-2.0/schemas/
